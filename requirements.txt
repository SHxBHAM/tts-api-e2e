voxcpm>=2.0.3
torch>=2.1.0
soundfile>=0.13.0
safetensors>=0.4.0
numpy>=1.24.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
huggingface_hub>=0.23.0
lameenc>=1.7.0
# Forced alignment (known text -> per-word timestamps). Pulls in torchaudio + transformers.
whisperx>=3.1.1
# RunPod Serverless worker SDK (handler.py). Harmless for the FastAPI server too.
runpod>=1.6.0
